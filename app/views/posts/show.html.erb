<p id="notice"><%= notice %></p>

<p>
  <b>Name:</b>
  <%= @post.name %>
</p>

<p>
  <b>Body:</b>
  <%= raw textilize(coderay(@post.body)) %>
</p>

<p>
  <b>Tags:</b>
  <% @post.tags.each do |tag|%>
    <%= tag.name %>
  <% end -%>
</p>


<%= link_to 'Edit', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
